{
  "version": "2.0.0",
  "tasks": [
    {
      // Fallback trigger for the same reset (Terminal > Run Task > Reset Booth,
      // or bind a key). The status-bar button above is the no-keyboard path.
      "label": "Reset Booth",
      "type": "shell",
      "command": "bash \"${workspaceFolder}/scripts/reset.sh\"",
      "presentation": {
        "reveal": "always",
        "panel": "shared",
        "clear": true
      },
      "problemMatcher": []
    }
  ]
}
